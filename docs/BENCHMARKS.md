# Benchmarks

No benchmark numbers are published until measured on a real disk. Use
`com.redislite.bench.BenchmarkClient` and `RecoveryBenchmark`; record CPU, RAM,
filesystem, Java version, workload, warmup, and median of three runs.
